<UserControl x:Class="ComputerConfiguration.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ComputerConfiguration.Views"
             xmlns:behaviors="clr-namespace:ComputerConfiguration.Behaviors"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" >
    <StackPanel>
        <StackPanel x:Name="stpLogin" DataContext="{Binding EntryDTO}">
            <Label Content="Email"/>
            <TextBox Text="{Binding Email}"/>
            <Label Content="Пароль"/>
            <PasswordBox>
                <i:Interaction.Behaviors>
                    <behaviors:PasswordBoxBehavior SecurePassword="{Binding Password, Mode=TwoWay}"/>
                </i:Interaction.Behaviors>
            </PasswordBox>
        </StackPanel>

        <Button Content="Войти" Command="{Binding LoginCommand}" CommandParameter="{Binding DataContext, ElementName=stpLogin}"/>
    </StackPanel>
</UserControl>
