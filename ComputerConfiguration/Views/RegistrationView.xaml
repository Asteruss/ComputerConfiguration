<UserControl x:Class="ComputerConfiguration.Views.RegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ComputerConfiguration.Views"
             xmlns:behaviors="clr-namespace:ComputerConfiguration.Behaviors"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d">
    <StackPanel>
        <StackPanel x:Name="stpRegistration" DataContext="{Binding RegistrationDTO}">
            <Label Content="Ваш email"/>
            <TextBox Text="{Binding Email}"/>
            <Label Content="Ваше имя"/>
            <TextBox Text="{Binding Name}"/>

            <Label Content="Придумайте пароль"/>
            <PasswordBox>
                <i:Interaction.Behaviors>
                    <behaviors:PasswordBoxBehavior SecurePassword="{Binding Password, Mode=TwoWay}"/>
                </i:Interaction.Behaviors>
            </PasswordBox>
            <Label Content="Повторите его"/>
            <PasswordBox>
                <i:Interaction.Behaviors>
                    <behaviors:PasswordBoxBehavior SecurePassword="{Binding PasswordRepeat, Mode=TwoWay}"/>
                </i:Interaction.Behaviors>
            </PasswordBox>
        </StackPanel>

        <Button Content="Зарегистрироваться" 
                Command="{Binding RegisterCommand}"
                CommandParameter="{Binding DataContext, ElementName=stpRegistration}"
                Style="{StaticResource RoundedButtonPrimary}"
                Width="300"/>
    </StackPanel>
</UserControl>
