<UserControl x:Class="ComputerConfiguration.Views.AddressListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ComputerConfiguration.Views"
             xmlns:controls="clr-namespace:ComputerConfiguration.Styles.Controls"
             mc:Ignorable="d" >
    <StackPanel>
        <Label Content="Мои адреса"/>
        <ItemsControl ItemsSource="{Binding Addresses}" HorizontalAlignment="Left">
            <ItemsControl.Template>
                <ControlTemplate  TargetType="ItemsControl">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  Width="1000">

                        <ItemsPresenter/>
                    </ScrollViewer>
                </ControlTemplate>
            </ItemsControl.Template>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="2" 
                                CornerRadius="5"
                                BorderBrush="Gray">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" 
                                   Text="{Binding FullAddress}" 
                                   TextWrapping="Wrap"
                                   FontSize="14"/>

                            <controls:IconButton Grid.Column="1"
                                             Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                             CommandParameter="{Binding}"
                                             Icon="/Icons/edit.png"
                                             Size="30"/>

                            <controls:IconButton Grid.Column="2"
                                             Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                             CommandParameter="{Binding}"
                                             Icon="/Icons/trash.png"
                                             Size="30"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button HorizontalAlignment="Left"
                Content="Добавить адрес"
                Command="{Binding AddCommand}"
                Style="{StaticResource RoundedButtonPrimary}"/>
    </StackPanel>
</UserControl>
