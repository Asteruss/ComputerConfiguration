<UserControl x:Class="ComputerConfiguration.Views.OrderTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ComputerConfiguration.Views"
             xmlns:converters="clr-namespace:ComputerConfiguration.Converters"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:OrderStatusConverter x:Key="OrderStatusConverter"/>
    </UserControl.Resources>

    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Orders}" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  IsReadOnly="True">

            <DataGrid.Columns>
                <DataGridTextColumn Header="№ заказа" Binding="{Binding Id}" Width="*"/>
                <DataGridTextColumn Header="Дата создания" Binding="{Binding CreationDate, StringFormat=dd.MM.yyyy HH:mm}" Width="2*"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Price, StringFormat={}{0:C}}" Width="2*"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding OrderStatus, Converter={StaticResource OrderStatusConverter}}" Width="2*"/>
                <DataGridTemplateColumn Header="Адрес доставки" Width="5*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Address.FullAddress}" 
                                       TextWrapping="Wrap"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
