<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:ComputerConfiguration.Styles.Controls">
    <Style TargetType="controls:IconButton" BasedOn="{StaticResource iconButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:IconButton">
                    <Border Background="{TemplateBinding Background}"
                        BorderBrush="LightGray"
                        BorderThickness="0"
                        CornerRadius="5">
                        <Image x:Name="PART_Image"
                           Source="{TemplateBinding Icon}"
                           Style="{StaticResource Icon}"
                           Width="{TemplateBinding Size}"
                           Height="{TemplateBinding Size}"
                           RenderTransformOrigin="0.5,0.5">
                            <Image.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Image.RenderTransform>
                        </Image>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="PART_Image"
                                                     Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                     To="1.1" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="PART_Image"
                                                     Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                     To="1.1" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="PART_Image"
                                                     Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                     To="1" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="PART_Image"
                                                     Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                     To="1" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>